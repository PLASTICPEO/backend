{
  "name": "backendlearn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:ui": "rm -rf build && cd ../style-react-app && npm run build && cp -r build ../backendlearn",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "json-server": "^0.17.1",
    "mongoose": "^6.10.0",
    "morgan": "^1.10.0",
    "render": "^0.1.4"
  }
}
